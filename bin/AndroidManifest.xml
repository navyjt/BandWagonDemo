<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laomaizi.bandwagondemo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".StartupActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/login_name" >
        </activity>
        <activity
            android:name=".UserSettingActivity"
            android:label="@string/login_name" >
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/reg_name" >
        </activity>
        <activity
            android:name=".MainFunctionActivity"
            android:label="@string/mainfunction_name" >
        </activity>
        <activity
            android:name=".lightpainting.LPMainActivity"
            android:label="@string/lightpainting" >
        </activity>
        <activity android:name=".lightpainting.StartShowActivity" >
        </activity>
    </application>

</manifest>